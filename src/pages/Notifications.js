import React from "react";
import {IonBackButton, IonButtons, IonContent, IonHeader, IonTitle, IonToolbar} from "@ionic/react";
import BoxDisplay from "./Boxes";

const SubscriptionsDisplay: React.FunctionComponent<Props> = ({branches, match, goBack}) => {

    return (

        <IonHeader>
            <h1>My Subscriptions Page Comes Here </h1>
        </IonHeader>


    );
};

export default SubscriptionsDisplay;