export interface Box {
    id: number,
    name: string,
    // profilePic: string,
    // twitter: string,
    about: string,
    // location: string,
    // email: string,
    // phone: string
}

export interface BranchState {
    branches: Box[]
}
